# Vite build output (run `npm run build` in frontend/ to regenerate)
/dist

# The admin console secret - generated per install, never committed.
.admin-token

# The map tile pyramid - ~100 MB, built from the client by tools/build_map_tiles.py.
/tiles
